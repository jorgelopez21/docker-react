sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t jorgelopez21/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true jorgelopez21/docker-react npm run test -- --coverage
